export const add = (val: number) => {
  return val + 1;
};

export const sub = (val: number) => {
  return val + 1;
};

export const mul = (val: number) => {
  return val * 10;
};

export const divi = (val: number) => {
  return val / 10;
};

export default { add, sub, mul, divi };
